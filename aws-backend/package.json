{
  "name": "bidintel-aws-backend",
  "version": "0.1.0",
  "private": true,
  "description": "AWS Lambda backend for BidIntel — incremental port of the Supabase edge functions",
  "workspaces": ["functions/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "package": "npm run package --workspaces"
  },
  "engines": { "node": ">=20" }
}
