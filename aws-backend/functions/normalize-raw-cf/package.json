{
  "name": "@bidintel-aws/normalize-raw-cf",
  "version": "0.1.0",
  "private": true,
  "description": "Normalizes raw_contracts_finder into app tables (cron) — Lambda port, DB stubbed",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "package": "npm run build && cd dist && zip -qr ../normalize-raw-cf.zip ."
  },
  "engines": { "node": ">=20" },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.16.5",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.3"
  }
}
