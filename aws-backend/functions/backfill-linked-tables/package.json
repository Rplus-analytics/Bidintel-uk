{
  "name": "@bidintel-aws/backfill-linked-tables",
  "version": "0.1.0",
  "private": true,
  "description": "One-off linked-table backfill — script-first, HTTP handler disabled by default",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "package": "npm run build && cd dist && zip -qr ../backfill-linked-tables.zip ."
  },
  "engines": { "node": ">=20" },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.16.5",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.3"
  }
}
