{
  "name": "@bidintel-aws/semantic-search",
  "version": "0.1.0",
  "private": true,
  "description": "Hybrid semantic + keyword + CPV tender search \u2014 Lambda port (DB stubbed)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "package": "../../scripts/build-lambda.sh semantic-search"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.16.5",
    "typescript": "^5.8.3",
    "@types/pg": "^8.11.10"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "@aws-sdk/client-secrets-manager": "^3.700.0"
  }
}
